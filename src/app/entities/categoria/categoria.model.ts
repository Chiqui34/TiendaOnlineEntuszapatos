export class Categoria {

   titulo: string;
   url: string;
   oferta: string;

   constructor(titulo: string, url: string, oferta: string) {
      this.titulo = titulo;
      this.url = url;
      this.oferta = oferta;
   }
}
